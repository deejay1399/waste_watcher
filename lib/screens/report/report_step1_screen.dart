import 'package:flutter/material.dart';

class ReportStep1Screen extends StatelessWidget {
  const ReportStep1Screen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Step 1 — coming next!')));
}
